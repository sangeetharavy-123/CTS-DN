<?xml version="1.0" encoding="UTF-8"?>

<!-- Root element for Spring Bean Configuration -->
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
       http://www.springframework.org/schema/beans
       https://www.springframework.org/schema/beans/spring-beans.xsd">

    <!-- BookRepository Bean -->
    <!-- Spring creates an object of BookRepository -->
    <bean id="bookRepository"
          class="com.library.repository.BookRepository"/>

    <!-- BookService Bean -->
    <!-- Spring creates an object of BookService -->
    <bean id="bookService"
          class="com.library.service.BookService">

        <!-- Setter Dependency Injection -->
        <!-- Injects the BookRepository bean into BookService -->
        <property name="bookRepository"
                  ref="bookRepository"/>

    </bean>

</beans>